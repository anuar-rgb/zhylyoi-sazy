-- =====================================================================
-- culture_events: организатор на двух языках
--
-- На странице мероприятия организатор показывается наравне с местом и
-- датой, и у него разные формулировки: «Жылыой сазы» ансамблі против
-- «Ансамбль "Жылыой сазы"». Одной колонки organizer для этого мало —
-- одна из версий сайта показывала бы чужой язык.
--
-- Старая колонка organizer остаётся нетронутой: удаление колонки
-- разрушающее и делается отдельным шагом.
-- =====================================================================

begin;

alter table public.culture_events
  add column if not exists organizer_kk text,
  add column if not exists organizer_ru text;

commit;
