-- =====================================================================
-- culture_news: метка на двух языках
--
-- Метка на карточке новости разная на двух языках: «Байқау» против
-- «Конкурс», «Үйірме» против «Кружок». С одной колонкой tag одна из
-- языковых версий сайта показывала бы чужой язык.
--
-- Старая колонка tag остаётся нетронутой: удаление колонки разрушающее
-- и делается отдельным шагом.
-- =====================================================================

begin;

alter table public.culture_news
  add column if not exists tag_kk text,
  add column if not exists tag_ru text;

commit;
